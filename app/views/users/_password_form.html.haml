= semantic_form_for @user, url: update_password_user_path(@user.id), html: { method: :put } do |f|

  = f.semantic_errors

  = f.inputs do
    = f.input :password, label: true
    = f.input :password_confirmation, label: true

  = f.actions do
    = f.action :submit, label: true
